# === DB (local) ===
spring.datasource.url=jdbc:postgresql://localhost:5432/leadsyncpro_db
spring.datasource.username=erdem
spring.datasource.password=erdem123
spring.datasource.driver-class-name=org.postgresql.Driver

# === JPA & log (local) ===
spring.jpa.show-sql=true
logging.level.org.springframework.security=DEBUG
logging.level.com.leadsyncpro=DEBUG

# === CORS/Frontend (local) ===
app.frontend.base-url=http://localhost:3000
app.cors.allowed-origins=http://localhost:3000

# === JWT & Encryption (local örnek de?erler) ===
app.jwt.secret=local-dev-secret-change-me
app.jwt.expiration.ms=86400000
app.encryption.key=local-dev-encryption-key-32-chars

# === OAuth (local bo? b?rak ya da dummy) ===
spring.security.oauth2.client.registration.google.client-id=
spring.security.oauth2.client.registration.google.client-secret=
spring.security.oauth2.client.registration.facebook.client-id=
spring.security.oauth2.client.registration.facebook.client-secret=

# === Mail (local dev; gerekmezse bo? b?rak) ===
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=
spring.mail.password=
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
app.mail.from=no-reply@local.dev

# === Flyway (local ihtiyaç varsa aç) ===
spring.flyway.enabled=false
spring.flyway.baseline-on-migrate=false
