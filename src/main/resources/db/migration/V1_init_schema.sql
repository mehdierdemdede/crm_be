-- ========================================
--  Flyway Baseline: Initial Schema
-- ========================================

-- Bu dosya mevcut tablo yapısını baseline olarak işaretler.
-- Flyway bundan sonraki versiyonları otomatik takip eder.

-- Örnek (isteğe bağlı tablo oluşturma)
-- CREATE TABLE example_table (
--     id UUID PRIMARY KEY,
--     name VARCHAR(255),
--     created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
-- );

-- Flyway versiyon kontrol tablosu oluşturulacak: flyway_schema_history
